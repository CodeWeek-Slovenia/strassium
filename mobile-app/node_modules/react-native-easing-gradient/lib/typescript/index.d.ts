import { EasingFunction } from 'react-native';
interface ColorStops {
    [location: number]: {
        color: string;
        easing?: EasingFunction;
    };
}
interface GradientParams {
    colorStops: ColorStops;
    easing?: EasingFunction;
}
declare function easeGradient({ colorStops, easing }: GradientParams): {
    colors: string[];
    locations: number[];
};
export { easeGradient };
